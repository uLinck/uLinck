name: generate animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *" 
  
  # allows to manually run the job at any time
  workflow_dispatch:
  
  # run on every push on the main branch
  push:
    branches:
    - main
    
  

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
          
      # computes current + longest contribution streak via the GraphQL API and renders a
      # static SVG card, so the README streak card never depends on a third-party render service
      - name: generate streak.svg
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -s -H "Authorization: bearer $GH_TOKEN" -H "Content-Type: application/json" \
            -d '{"query":"query($login:String!){user(login:$login){contributionsCollection{contributionCalendar{weeks{contributionDays{contributionCount}}}}}}","variables":{"login":"${{ github.repository_owner }}"}}' \
            https://api.github.com/graphql > contributions.json

          python3 <<'PYEOF'
          import json

          with open("contributions.json") as f:
              data = json.load(f)

          weeks = data["data"]["user"]["contributionsCollection"]["contributionCalendar"]["weeks"]
          days = [d["contributionCount"] for w in weeks for d in w["contributionDays"]]

          i = len(days) - 1
          if days[i] == 0:
              i -= 1  # today may not have contributions yet, don't break the streak on it

          current = 0
          while i >= 0 and days[i] > 0:
              current += 1
              i -= 1

          longest = run = 0
          for count in days:
              run = run + 1 if count > 0 else 0
              longest = max(longest, run)

          svg = f'''<svg width="400" height="195" viewBox="0 0 400 195" xmlns="http://www.w3.org/2000/svg">
            <rect x="0.5" y="0.5" width="399" height="194" rx="10" fill="#000000" stroke="#ff00f6"/>
            <circle cx="140" cy="95" r="48" fill="none" stroke="#ff00f6" stroke-width="5"/>
            <text x="140" y="90" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="32" font-weight="700" fill="#ffffff">{current}</text>
            <text x="140" y="112" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="11" fill="#8B8B8B">days</text>
            <text x="140" y="160" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="13" fill="#bb00b4">Current Streak</text>
            <text x="300" y="90" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="32" font-weight="700" fill="#ffffff">{longest}</text>
            <text x="300" y="112" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="11" fill="#8B8B8B">days</text>
            <text x="300" y="160" text-anchor="middle" font-family="'Segoe UI', Ubuntu, sans-serif" font-size="13" fill="#bb00b4">Longest Streak</text>
          </svg>'''

          with open("dist/streak.svg", "w") as f:
              f.write(svg)
          PYEOF

      # push the content of <build_dir> to a branch
      # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
